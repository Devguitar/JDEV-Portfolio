<section id="hero" class="border-b-2 border-black relative overflow-hidden">
  <!-- Grid Background -->
  <div class="absolute inset-0 opacity-[0.08] pointer-events-none">
    <div
      class="w-full h-full"
      style="background-image: 
        linear-gradient(#000 2px, transparent 2px), 
        linear-gradient(90deg, #000 2px, transparent 2px); 
       background-size: 410px 400px;
       background-position: -72px 0;"
    >
    </div>
  </div>

  <!-- Gradient Overlay - Left to Right -->
  <div
    class="absolute inset-0 pointer-events-none"
    style="background: linear-gradient(90deg, rgba(255,255,255,1) 0%, rgba(240,240,240,0.8) 50%, rgba(0,0,0,0.05) 100%);"
  >
  </div>

  <div class="relative z-10 px-4 sm:px-6 md:px-8 lg:px-12 xl:px-16 py-8 min-h-screen">
    <div class="max-w-2xl pt-32 sm:pt-40 md:pt-48 lg:pt-64 xl:pt-80">
      <div>
        <h1
          class="text-4xl sm:text-6xl md:text-7xl lg:text-8xl font-light leading-[0.95] tracking-tight"
        >
          Hello!
          <span class="block font-semibold mt-2 inline-block"
            >I'm John Dave</span
          >
        </h1>

        <!-- Subtitle aligned to the right -->
        <p class="text-lg md:text-xl text-gray-700 font-medium mt-6 sm:mt-8 md:mt-10">
          Junior Full-Stack Developer
        </p>
      </div>
    </div>
  </div>
</section>